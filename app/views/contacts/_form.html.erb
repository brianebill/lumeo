<div class="row span7 community">
   <%= simple_form_for @contact do |f| %>
      <%= f.input :subject, :label => false, placeholder: "Subject: What is your message regarding?" %>
	  <%= f.input :message, :label => false, placeholder: "Body: Send a private message to Lumeo" %>
      <div class ="form-actions">
         <%= f.submit 'Send Message', :class => 'btn btn-primary pull-right' %>
      </div>
   <% end %>
</div>