<% if @questions.total_entries.zero? %> 
 <p>Your search returned no Course Requests. Step 2: Click on the blue button to submit your Course Request</p>

<% elsif @search.nil? %>
	<div id="problems_table"><%= render 'questions' %></div>

<% else %>

<h3 class="center">Your search found <%= pluralize(@questions.total_entries, "question") %>....</h3>
<div id="requests">
	   <%= render 'questions' %>
</div>
<h3 class="center">Enter a new request by clicking on the blue button</h3>
   
<% end %>