<%= render 'title' %>

<%= render 'index_search_form' %>

<%= render 'search_conditional' %>

<%= button_to 'New Question', new_question_path, :method => 'get', :class => 'btn btn-large btn-primary mleft20 mtop20 pull-right' %>