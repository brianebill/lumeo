<%= render 'title' %>

<%= render 'index_search_form' %>

<%= render 'search_conditional' %>

<%= button_to 'New Request', new_request_path, :method => 'get', :class => 'btn btn-large btn-primary mleft20 mtop20 pull-right' %>