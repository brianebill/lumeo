    <%= link_to "Add to My Courses", "#", :class => 'btn'  %>
<% if can? :update, @course %>	
    <%= link_to 'Edit', edit_course_path(@course), :class => 'btn' %>
    <%= link_to 'Delete', course_path(@course), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
<% end %>