<fieldset>
	<%= f.input :name, :label => 'Subject', :placeholder => 'Add a subject like, "Film"' %>
	<%= f.hidden_field :_destroy %>
	<%= link_to 'remove subject', '#', class: 'remove_fields pull-right' %>
</fieldset>

