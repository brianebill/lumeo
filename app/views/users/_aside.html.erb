<section>
	<h3 class="center user_name"><%= @user.name %></h3>
	<p class="padding-bottom"><%= image_tag @user.photo.url(:show) %></p>
</section>
	<span>
		<%= @user.name %>'s activity:<br /> <%= pluralize(@user.microposts.count, "reflections") %>
	</span>
<section>
	<%= render 'shared/stats' %>
</section>