<section>
	<h3 class="center user_name"><%= @user.name %></h3>	
		<% if @user.image %>
		 <%= image_tag @user.image.source %>
		<% end %>
</section>
	<span>
		<%= @user.name %>'s activity:<br /> 
		<%= pluralize(@user.microposts.count, "reflections") %>
		<%= user_votes_count %>
	</span>
<section>
	<%= render 'shared/stats' %>
</section>