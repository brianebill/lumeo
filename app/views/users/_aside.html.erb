<section>
	<h3 class="center user_name"><%= @user.name %></h3>	
		<% if current_user.images.any? %>
      		<p class="padding-bottom"><%= image_tag @user.images.first.source(:show) %></p>
    	<% end %>
</section>
	<span>
		<%= @user.name %>'s activity:<br /> <%= pluralize(@user.microposts.count, "reflections") %>
	</span>
<section>
	<%= render 'shared/stats' %>
</section>